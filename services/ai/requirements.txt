fastapi==0.111.0
uvicorn==0.30.1
# Required for the multipart audio upload on POST /transcribe (DEV-31).
python-multipart==0.0.32
faster-whisper==1.1.1
# faster-whisper 1.1.1 imports `requests` directly, but huggingface-hub 1.x dropped it as a
# transitive dep (moved to httpx). Pin it explicitly so the import resolves.
requests==2.32.3
httpx==0.27.0
azure-monitor-opentelemetry==1.6.4
# The on-prem advisory model runtime (llama-cpp-python, DEV-83) is NOT installed in the base
# image. Nothing calls the advisory endpoint yet (it fails open to UNAVAILABLE without the
# runtime), and the mini-PC benchmark installs it in a separate venv (benchmark/README.md). The
# runtime and the GGUF weights are added when the advisory is wired end-to-end and deployed.
