fastapi==0.111.0
uvicorn==0.30.1
faster-whisper==1.1.1
httpx==0.27.0
azure-monitor-opentelemetry==1.6.4
# On-prem advisory model runtime (DEV-83). CPU-only GGUF inference; keeps note text on the box.
# Verify the pin against the release list before merge.
llama-cpp-python==0.3.2
